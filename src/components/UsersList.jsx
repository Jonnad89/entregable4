import React from 'react';

const UsersList = () => {
    return (
        <div>
            <button>Delete</button>
            <button>Edit</button>
        </div>
    );
};

export default UsersList;